@model PagedList.IPagedList<WineShop.Models.LoaiSanPham>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Danh sách loại sản phẩm";
    Layout = "~/Areas/Administrator/Views/Shared/_Layout.cshtml";
}

<center><h1>LOẠI SẢN PHẨM</h1></center>

<div class="">
    <div class="page-title">
        <i class="icon-custom-new"></i>
        <h3></h3>
        <div class="pull-right actions">
            <button class="btn btn-primary btn-cons" type="button" id="btn-new-ticket">@Html.ActionLink("Thêm mới", "ThemMoi")</button>
        </div>
    </div>
    <div class="clearfix"></div>
</div>
<br />

Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) của @Model.PageCount
@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))

<div style="background-color:white">
    <table class="table table-hover no-more-tables">
        <tr>
            <th>
                 @Html.ActionLink("Loại sản phẩm", "Index", new { sortOrder = ViewBag.TenLoaiSanPhams })
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                    <a href="@Url.Action("ChiTiet", "LoaiSanPham", new { id = item.MaLoaiSanPham})">
                        @Html.DisplayFor(modelItem => item.MaLoaiSanPham)
                    </a>
                </td>*@
                <td> 
                             
                    <h4 style="margin-left:100px"> @Html.DisplayFor(modelItem => item.TenLoaiSanPham)</h4>
                </td>

                <td width="50">
                
                        <a class="remove" href="@Url.Action("CapNhat", "LoaiSanPham", new { id = item.MaLoaiSanPham})">
                            <h1><i class="fa fa-retweet"></i></h1>
                        </a>
                        
                </td>
                <td>
                        <a class="remove" href="@Url.Action("Xoa", "LoaiSanPham", new { id = item.MaLoaiSanPham})">
                            <h1><i class="fa fa-times"></i></h1>
                        </a>
                  
                   
                </td>
            </tr>
        }

    </table>

</div>