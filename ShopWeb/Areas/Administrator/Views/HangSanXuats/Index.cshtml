@model IEnumerable<ShopWeb.Areas.Administrator.Models.HangSanXuat>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Administrator/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenHangSanXuat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LogoURL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BiXoa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenHangSanXuat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LogoURL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BiXoa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHangSanXuat }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHangSanXuat }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHangSanXuat })
        </td>
    </tr>
}

</table>
