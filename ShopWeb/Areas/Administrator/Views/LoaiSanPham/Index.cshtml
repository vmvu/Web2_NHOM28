@model PagedList.IPagedList<WebShop.Areas.Administrator.Models.LoaiSanPham>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Administrator/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm mới", "ThemMoi")
</p>

Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))

<div style="background-color:white">
    <table class="table table-hover no-more-tables">
        <tr>
            @*<th>
                Mã loại
            </th>*@
            <th>
                @Html.ActionLink("Loại sản phẩm", "Index", new { sortOrder = ViewBag.TenLoaiSanPhams })
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                    <a href="@Url.Action("ChiTiet", "LoaiSanPham", new { id = item.MaLoaiSanPham})">
                        @Html.DisplayFor(modelItem => item.MaLoaiSanPham)
                    </a>
                </td>*@
                <td>
                    <a href="@Url.Action("ChiTiet", "LoaiSanPham", new { id = item.MaLoaiSanPham})">
                        @Html.DisplayFor(modelItem => item.TenLoaiSanPham)
                    </a>
                </td>

                <td>
                    @Html.ActionLink("Cập nhật", "CapNhat", new { id = item.MaLoaiSanPham }) |
                    @Html.ActionLink("Xóa", "Xoa", new { id = item.MaLoaiSanPham })
                </td>
            </tr>
        }

    </table>

</div>